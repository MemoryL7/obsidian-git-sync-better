name = "cc-obsidian-sync"
main = "src/index.ts"
compatibility_date = "2026-01-01"

# 部署前先创建 bucket:npx wrangler r2 bucket create obsidian-vault
[[r2_buckets]]
binding = "VAULT"
bucket_name = "obsidian-vault"

# 鉴权 token 用 secret 注入,不要写在这里:
#   npx wrangler secret put AUTH_TOKEN
